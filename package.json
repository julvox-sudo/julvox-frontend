{
  "name": "julvox-frontend",
  "version": "17.0.0",
  "private": true,
  "scripts": {
    "verify:source": "node scripts/verify-source.js",
    "build": "npm run verify:source && node build-static.js && node scripts/verify-dist.js",
    "verify:dist": "node scripts/verify-dist.js"
  },
  "engines": {
    "node": ">=18"
  }
}
