# F001 — Validation attendue

La pull request F001 doit rester en brouillon tant que la preuve visuelle suivante n’a pas été fournie :

- le workflow `Frontend baseline` est terminé ;
- `Build static frontend` est vert ;
- aucune vérification n’est rouge ;
- le commit contrôlé correspond à la tête actuelle de la branche.

F001 ne modifie pas le runtime. La validation confirme que la documentation et le contrôle de caractérisation n’ont pas cassé le build historique.

Aucune extraction de code ne commence avant cette validation.
